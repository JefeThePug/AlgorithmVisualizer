const DarkBG = "#0B192C"
const LightBG = ""

function setup() {
    createCanvas(1200, 600); // inside <main>
    imageMode(CENTER);
    textAlign(CENTER, CENTER);
}

function draw() {
    background(DarkBG);
}
